<div class="cf" id="gallery">
  <% @images.each do |image| %>
    <%= link_to content_tag(:div, "", style: "background-image: url(#{image.image.url})"), image, remote: true, class: "image" %>
  <% end %>
</div>

<div id="modal"></div>
