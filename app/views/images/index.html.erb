<div class="cf" id="gallery">
  <% @images.each do |image| %>
    <%= link_to content_tag(:div, "", style: "width: 100px; height: 100px; background-image: url(#{image.image.url})"), image, remote: true, class: "Image" %>
  <% end %>
</div>

<div id="modal"></div>
