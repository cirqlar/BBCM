Subscribe
<div class="cf" id="sub_create">

</div>
<%= form_for @subscriber, remote: true do |f| %>
  <%= render 'shared/errors', object: f.object %>

  <%= f.label :name %>
  <%= f.text_field :name%>

  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.submit 'Subscribe' %>
<% end %>
