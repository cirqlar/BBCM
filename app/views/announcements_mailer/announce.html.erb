<h1><%= @an.title %></h1>

<h3>Dear <%= @suber.name.split.map(&:capitalize).join(' ') %></h3>

<p>
  <%= @an.desc %>
</p>

<%= link_to 'Unsubscribe', subs_destroy_url(@suber, token: @suber.token), target: "_blank" %>
