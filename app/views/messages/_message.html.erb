<div class="message">
  <div class="poster" style="background-image: url(<%= message.poster.url %>)"></div>
  <div class="descCont">
    <%= link_to message.title, show_message_path(:id => message), class: 'title'%>
    <span class="date"><%= message.updated_at %></span>
    <span class="type"><%= message.content_type %></span>
  </div>
  <%= link_to "delete", message, method: :delete, data: {confrim: "Are you sure?"} %>
</div>
